const { Pi, getArraySum } = require('./lib/getArraySum.js')
const { x, y } = require('./lib/var.js')

module.exports = {
    x,
    y,
    Pi,
    getArraySum,
}

// console.log(x, y, Pi, getArraySum([1, 2, 3, 4]));